/**
 * Realizar la función esPalindromo(cadena) de manera recursiva. 
 * @param {*} cadena 
 */

function esPalindromo(cadena) {
    let str = cadena;
    console.log(str.length)
    while (str.length) {
        str = str.length - 1;
    }
}

esPalindromo("hola buenos días")


var string = "Hola buenos dias"
console.log(string);